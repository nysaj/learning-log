# 2026-07-18

> 学習時間：4.75時間（06:18〜06:53、16:00〜17:33、18:00〜20:20、21:16〜21:44）

## 今日の学習
* `9.コントローラとデータベースの連携`、`10.モデルを使った処理`、`11.マイグレーションについて`、`12.ダミーデータの作成`、`13.DBからデータを取得・表示`、`14.N+1問題`まで視聴
* 動画に沿ってファイル作成など実践
  - 画像の表示

## メモ
* `sail npm run dev`
  - Viteサーバーを起動（変更内容を自動で反映）
* `asset()`
  - 画像や動画のソースを入力して表示させる
* `Eloquent（エロクエント）`
  - SQLの代わりにPHPを使ってDBを操作する仕組み

## 気づき
* 以前の気づきにも記入したが、ある程度まとめて視聴したほうが理解しやすいように感じるので、この方針を継続する
* GPT Imageの予習ガイドは課題以外の内容まで含まれていたので、情報を整理するために作成し直す
* `sail npm run dev`で起動中に再度コマンド入力するとエラーが出る
  - 動いているターミナルで`Ctrl + C`を押して停止
  - どのターミナルで動いているか不明の場合は、
    1. `sail down`でdockerを停止
    2. `sail up -d`でdockerを再起動
    3. 別のターミナルで`sail npm run dev`コマンドを入力
* ディレクティブの入力で`@@`の表示のままになっていたり、文末の`;`が未入力でエラーが起きていたので、構文エラーにならないように意識する

## 次回の学習へ
* 残りの課題動画の実習を進める
