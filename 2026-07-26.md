# 2026-07-26

> 学習時間：4.5時間（13:45〜16:15、17:27〜19:31）

## 今日の学習
* 投稿データの作成と保存についての動画視聴

## 疑問点と解決内容
* `Route::get('/posts/create', CreatePost::class)->name('posts.create')`
    - `name('posts.create')`が何を指しているのかわからなかった（posts.createの名前のファイルもなかった）
        - /posts/createにアクセスしたらCreatePostクラスを呼び出して、そのURL（/posts/create）にposts.createと名前を付けている
        - URLが変更になってもposts.create名で呼び出せる（変更する必要がない）
    - `CreatePost::class`が何をしているかわからなかった
        - CreatePostというclassを呼び出している
        - 画面表示や保存などの処理をする

* `Post::create()`が何を指していて、何をしているかわからなかった
    - app/Models/Post.phpファイルのこと
    - `class Post extends Model`で親のModelから継承し、`create()`を実行している
    - postsテーブルに()の中身を保存する

* `<form wire:submit="save" class="space-y-6">`
    - submitで`save`という関数（メソッド）を実行している

* `<flux:input wire:model="title" label="タイトル" placeholder="記事のタイトルを入力" />`
    - titleはCreatePostクラスの`public $title = ""`の中身と連動している
    - この場合は空欄

## 気づき
* コードの処理の流れを一つずつ追っていくことで、流れがわかり、理解につながった
* ChatGPTとやりとりした学習手順にそって進めることで、無駄な重複を避ける事ができ、効率よく学習できていると感じる
* 動画を一度最後まで見てから実装することで、全体の流れを意識しながら進められた

## 次回の学習へ
* 学習手順にそって進める
* ChatGPTが想定した時間よりかかっているので、学習手順は都度見直していく
