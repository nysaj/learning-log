# 2026-07-28

> 学習時間：5.5時間（03:43〜06:27、08:44〜11:19）

## 今日の学習
* 記事の編集機能の実装

## 疑問点と解決内容
* 編集ボタンが一部の記事でしか動作していなかった
    - ユーザーIDとログイン中のユーザーIDを比較すべきところを、記事IDとログイン中のユーザーIDで比較していた
    - 一致した記事のみ編集できて、他の記事が編集できなくなっていた
    ```php
    if ($post->id !== Auth::id()) // 記事のidと比較している
    if ($post->user_id !== Auth::id()) // こちらが正解
    ```
* `$this->〇〇`のthisが具体的に何を指しているのか理解できていなかった
    - 今のオブジェクト自身を表している
    - オブジェクトが持っているプロパティやメソッドにアクセスしている
    - *オブジェクト指向*と大いに関係ある

* ```php
    public Post $post;
    $this->post->update()
  ```
    - `Post` は型の宣言（$postのプロパティにPostオブジェクトが入る）
    - postプロパティがupdate()メソッドを持っているのではない
    - *オブジェクトが持つpostプロパティの中にPostオブジェクトが入っていて、そのPostオブジェクトがupdate()メソッドを持つ*

* ```php
    $this->post->update([
            'title' => $this->title, 
            'body' => $this->body,
        ]);
  ```
    - `'title'`、`'body'`はデータテーブルのカラム名を示している
    - bladeファイルの変数ではない
    - データテーブルの内容を更新している

## 気づき
* 記事のID（id）と記事の所有者のID（user_id）は異なる

## 次回の学習へ
* ChatGPTの確認問題を解いて理解を深めていく
* 疑問点をやり取りすると時間がかかるので、数時間まとまった時間を確保して学習する
