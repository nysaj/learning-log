# 2026-07-31

> 学習時間：4.5時間（8:35〜13:00）

## 今日の学習
* Laravelの問題集（スマホアプリ）
* 最終調整（検索機能や見た目の調整など）の動画視聴 → 実装

## 問題点と解決内容
* 検索ボックスにワードを入れてもヒットしない
    - 20ページ目で検索していたので、検索結果が２ページ分しかなく画面に表示されていなかった
    - ```php
        updateSearch()
        ↓
        updatedSearch()
      ```
      - タイプミスで`d`が抜けていたため、検索ワードを入れても１ページ目に移動していなかった

* `<flux>`を`<flex>`と入力していて、直した際に閉じタグの方を修正し忘れてエラーになっていた
    - 似たような文字はタイプミスをしやすいので注意する

* 次のコードのデータの流れや意味をよく理解できていなかった
    ```php
        #[Url]
        wire:model.live="search"
    ```
    - Blade側で入力された値を、livewire側の`$search`にリアルタイムで同期する
    - `#[Url]`によって同期された値がURLの`?search=〇〇`と同期する
    - `#[Url]`がなければ検索結果は機能するが、URLは固定のまま

* `$this->search`が正しく、`search`、`$search`がだめな理由を説明できなかった
    - `$this->search`：そのオブジェクトが持っているプロパティ`$search`を示している
    - `search`：変数ではなく、文字列なのかも定義されていないので使用できない
    - `$search`：メソッドの中で定義されたローカル変数

## 気づき
* 時間の都合で動画の途中で切り上げると、思い出すのに時間がかかるので、なるべく区切りよく学習できるようにする
* エラーがでて解決するために色々と調べることが、理解につながっているように感じる
* 確認問題で自分の言葉で説明することで、曖昧な部分を知ることができた

## 次回の学習へ
* 確認問題を通じて、自分の言葉で説明できるようにしていく
* 課題を提出できるように進める