# 2026-08-01

> 学習時間：8.5時間（06:14〜06:48、16:35〜17:35、18:58〜02:55）

## 今日の学習
* 課題提出前のアレンジ
    - 検索総数の表示（PHP）
        - 全記事一覧ページ、自分の記事一覧ページ
    - トップへ戻るボタンの作成（JavaScript）
        - 全記事一覧ページ、自分の記事一覧ページ
        - スクロールを滑らかに
* もくもく会参加

## 問題点と解決内容、学んだこと
* Tailwind CSSのクラスの動作をよく理解していなかった
    - `justify-end`
        - `justify-right`と入力していた
    - `class=fixed`
        - 画面に固定表示する

* `count()`
    - 全件表示にならず、ページネーションで画面に表示されている件数10になっていた
    - `total()`を使用して全件表示

* `@vite`
    - CSSとJavaScriptを読み込む設定

* `const scrollPosition = window.scrollY`
    - このコードをapp.jsファイルの先頭の行に記入していたので、値が固定値０になっていて期待通りの動作にならなかった
    - イベントリスナーの中に配置して、スクロールされるたび値を取得するようにした

* `scrollY`
    - 画面の下方向にスクロールした量を表している

* `classList.add``.remove`
    - クラス属性にクラスを追加、削除できる
    - .delete()で削除ではない
    
* 課題のサイトのアレンジが再ログイン後、アレンジが反映されなくなった（CSS・JavaScriptが効いていない）
    - Appleシリコン用のファイル`@rollup/rollup-darwin-arm64`が不足していたのが原因
    - `node_modules`と`package-lock.json`を削除してから`npm install`→`npm run build`で直った

## 気づき
* 久しぶりにCSSやJavaScriptを記入して再確認することができた
* 課題のアレンジで、実際にやりたいことを調べて記述していくことで理解が深まったと感じる
* 思うように動かないエラー対応は、一つずつ絞り込んでいくことが重要だと感じた
* 今回の学習が一番身についた感覚がする

## 次回の学習へ
* 課題を提出する
* 学習を継続して遅れを取り戻せるようにする