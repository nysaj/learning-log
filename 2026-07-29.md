# 2026-07-29

> 学習時間：5時間（03:44〜06:13、08:12〜09:04、23:16〜01:01、02:15〜02:40）

## 今日の学習
* テストデータの作成（SeederとFactory）
* Laravelの問題集（スマホアプリ）

## 問題点と解決内容
* テストユーザーでログインすると、課題動画にはなかった二段階認証のページが出てきた
    - ```php
        $test_user = User::factory()
            ->withoutTwoFactor()
            ->create()
      ```
    - `withoutTwoFactor()`を入れることで二段階認証を回避できた
    - 環境（バージョン）の違いで最初から入っていた

* ```php
    User::factory()
  ```
    - 左と右の関係性がよくわかっていなかった
    - `User`クラスの`factory()`メソッドを使う
    - `UserFactory.php`の`definition()`を実行する
    - `Userオブジェクト（usersデーブルの１件分のデータ）を作るための設計図を取得する

* `->`と`=>`をなんとなくで使っていて、違いを理解できていなかった
    - `->` アロー演算子、アロー、矢印
        - 次の処理へつなぐ
        - `-> create()`なら処理を実行
        - `-> id`ならidを*取得*
    - `=>` ダブルアロー
        - 左の項目に右の値を入れる

* `Factory`が使用された時にどのコードが実行されているのか理解できていなかった
    - `Factory`内の`definition()`が呼び出される
    - `Factory`は１件のデータの設計図
    - `definition()`は設計図の中身（各カラムの値）を定義してLaravelに渡している

## 気づき
* なんとなくで記入していたあいまいな部分が、ChatGPTの理解度チェックの小問題でわかるようになってきた

## 次回の学習へ
* 理解度を完璧にするのはプログラミング学習において良くないと言われているが、最低限の理解はするように意識する
* なぜ？を大切にしながらも、深堀りしすぎないように注意する
